version '1.3.0'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':common')
    compile 'com.google.dagger:dagger:2.0'
    testCompile 'junit:junit:4.12'
}
